# Frontend

React/Vite frontend for the full-stack web app template.

This app uses Supabase Auth in the browser and calls the Express backend with a
Supabase bearer token for trusted work such as billing status, Stripe Checkout,
and Stripe customer portal sessions.

See the root `README.md` for full setup, architecture, environment variables,
and test notes.
